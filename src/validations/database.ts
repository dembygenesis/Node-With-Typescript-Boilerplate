import {Application, Router, Request, Response, NextFunction, ErrorRequestHandler} from "express";

export default class DatabaseValidations {
    static getRecords() {
        console.log();
    }
}
